cursor:
  style: Beam
    # Block / Underline / Beam

    # Values for 'binking'
    # Never (prevent the cursor from ever blinking) / Off / On / Always (force)
  blinking: On

  blink_interval: 800

    # When window is not focused, the cursor will be a hollow box
  unfocused_hollow: true
