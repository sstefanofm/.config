Plug 'easymotion/vim-easymotion'

" Shortcuts
"
" <leader><leader>  <- (prefix)
" -w ; -b ; -e   <- jump between words
" -s   <- search for one character
" I've remaped to search for 1 and 2 characters in the main init.vim file
" <leader>s searches for two characters in the whole window
" <leader>t searches for two characters below the cursor
